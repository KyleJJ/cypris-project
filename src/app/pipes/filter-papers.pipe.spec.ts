import { FilterPapersPipe } from './filter-papers.pipe';

describe('FilterPapersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPapersPipe();
    expect(pipe).toBeTruthy();
  });
});
